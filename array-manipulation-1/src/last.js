/* exported last */

/*
  - Create new variable to house last index
  - Access array at last index with bracket notation
  - Use array.length property - 1 to get last index
  - Return array
*/

function last(array) {
  var lastIndex = array[array.length - 1];
  return lastIndex;
}
