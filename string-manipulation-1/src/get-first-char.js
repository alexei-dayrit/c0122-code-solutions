/* exported getFirstChar */

// 1. Create first character variable
// 3. Access string at first index
// 2. Return first character variable

function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}
