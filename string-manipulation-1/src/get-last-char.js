/* exported getLastChar */

// 1. Create last character variable
// 2. Access string argument at it's length - 1
// 3. Return last character variable

function getLastChar(string) {
  var lastChar = string[string.length - 1];
  return lastChar;
}
