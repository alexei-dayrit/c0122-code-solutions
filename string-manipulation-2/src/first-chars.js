/* exported firstChars */

/*
  - Create variable
  - Access string from the start UP to length input
  - Return variable
*/

function firstChars(length, string) {
  var first = string.substring(0, length);
  return first;
}
